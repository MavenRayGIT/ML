# Copy to .env — never commit .env
# Cloudflare Pages: add the PUBLIC_* values in the dashboard too.

PUBLIC_GTM_ID=
PUBLIC_GA4_ID=
PUBLIC_CLARITY_ID=

# M&L admin edit drawer (embed.js). Set on STAGING only — production
# must never load the drawer. See src/layouts/Page.astro.
#   PUBLIC_ML_EDIT_ENABLED=true
#   PUBLIC_ML_ADMIN_ORIGIN=https://admin.mackandlee.com   (optional override)
PUBLIC_ML_EDIT_ENABLED=
PUBLIC_ML_ADMIN_ORIGIN=
