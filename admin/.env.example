# Phase -1: nothing the Astro static build needs at build time yet.
# Server-side values (basic auth password, future Cursor SDK key) live in
# Cloudflare Pages env vars for production, and in `.dev.vars` for local dev.
# See README "Local development" and "Deployment".
PUBLIC_ML_ENV=staging
